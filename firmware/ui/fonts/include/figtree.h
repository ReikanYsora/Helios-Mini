#pragma once

#include "lvgl.h"

/* Figtree (the Helios brand font, Medium 500), generated from the website's
 * woff2 via lv_font_conv. Set as the LVGL default in hardware/display, so
 * every widget uses it; the sizes below are for explicit larger text. */
extern const lv_font_t figtree_16;
extern const lv_font_t figtree_24;
extern const lv_font_t figtree_32;
extern const lv_font_t figtree_48;
