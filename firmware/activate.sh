#!/usr/bin/env bash
# Source this (don't execute) to set up the ESP-IDF environment for Helios
# Mini: `source firmware/activate.sh` from the repo root, or
# `. activate.sh` from inside firmware/.
#
# Assumes ESP-IDF v5.5.1 is cloned at ~/esp/esp-idf (see firmware/README.md
# for setup instructions if it isn't yet).
. "$HOME/esp/esp-idf/export.sh"
